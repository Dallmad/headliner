import styled from '@emotion/styled';

export const LoaderContainer = styled.div`
  position: absolute;
  top: 0;
  left: 50%;
  transform: translate(-50%, 0);
  display: flex;
  width: 4em;
  margin: auto;
  z-index: 25;
`;
